# 1Panel 部署指南

## 环境要求

- Linux 服务器（推荐 Ubuntu 22.04 / CentOS 8+）
- 1Panel 面板已安装（自带 Docker + Docker Compose）
- 至少 2GB 内存（Node.js 构建 + better-sqlite3 编译需要）
- 建议配置域名 + SSL 证书

---

## 一、服务器准备

### 1.1 SSH 登录服务器

```bash
ssh root@你的服务器IP
```

### 1.2 创建项目目录

```bash
mkdir -p /opt/wechat-exporter
cd /opt/wechat-exporter
```

---

## 二、获取代码

### 方式 A：从 GitHub 拉取（推荐）

```bash
git clone https://github.com/lwbupt/wechat-article-exporter.git .
```

### 方式 B：从本地上传

在本地机器执行：

```bash
# 打包代码（排除 node_modules、数据库等）
cd "D:/项目/claude code/wechat-exporter"
tar --exclude='node_modules' --exclude='.nuxt' --exclude='.output' \
    --exclude='data/wechat.db*' --exclude='data/kv' --exclude='data/excel' \
    --exclude='.agents' --exclude='.claude' --exclude='.windsurf' \
    -czf ../wechat-exporter.tar.gz .

# 上传到服务器
scp ../wechat-exporter.tar.gz root@你的服务器IP:/opt/wechat-exporter/
```

在服务器上解压：

```bash
cd /opt/wechat-exporter
tar -xzf wechat-exporter.tar.gz
rm wechat-exporter.tar.gz
```

---

## 三、配置环境变量

```bash
cp .env.example .env
vim .env
```

### 必须修改的配置

```bash
# 端口（宿主机映射端口，按需修改）
PORT=3000
```

### 按需配置（也可在应用内「设置」页面通过 UI 配置）

```bash
# AI 服务（内容生成功能必须）
AI_API_KEY=你的API密钥
AI_API_BASE=https://open.bigmodel.cn/api/paas/v4
AI_MODEL=glm-5

# 图片搜索（配图功能需要，至少配一个）
PEXELS_API_KEY=你的Pexels密钥
# 或
SERPER_API_KEY=你的Serper密钥

# 素材搜索（可选）
TAVILY_API_KEY=你的Tavily密钥
```

---

## 四、构建与启动

### 4.1 构建镜像

```bash
cd /opt/wechat-exporter
docker compose build
```

> 首次构建约 3-5 分钟，需要编译 better-sqlite3 原生模块。

### 4.2 启动服务

```bash
docker compose up -d
```

### 4.3 验证运行

```bash
# 查看容器状态
docker compose ps

# 查看日志
docker compose logs -f

# 测试访问
curl http://localhost:3000
```

看到 HTML 输出即表示启动成功。

---

## 五、1Panel 配置反向代理

### 5.1 安装 Nginx

在 1Panel 面板中：**应用商店 → 网站 → 安装 OpenResty/Nginx**（通常已预装）

### 5.2 创建网站

1. 1Panel 面板 → **网站** → **创建网站**
2. 选择 **反向代理**
3. 填写配置：
   - **域名**：`down.yourdomain.com`（你的实际域名）
   - **代理地址**：`http://127.0.0.1:3000`
4. 点击创建

### 5.3 配置 SSL 证书

1. 网站列表 → 点击刚创建的网站 → **HTTPS**
2. 选择 **申请证书**（Let's Encrypt 免费）或上传已有证书
3. 开启 **强制 HTTPS**

### 5.4 WebSocket 支持（可选）

如果后续需要 WebSocket，在网站配置的 Nginx 配置中添加：

```nginx
location / {
    proxy_pass http://127.0.0.1:3000;
    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection "upgrade";
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
}
```

---

## 六、DNS 解析

在你的域名服务商（如阿里云、Cloudflare）添加 DNS 记录：

| 类型 | 主机记录 | 记录值 |
|------|---------|--------|
| A | down | 你的服务器公网IP |

---

## 七、日常运维

### 查看日志

```bash
docker compose logs -f --tail=100
```

### 重启服务

```bash
docker compose restart
```

### 更新版本

```bash
cd /opt/wechat-exporter
git pull origin master
docker compose build
docker compose up -d
```

### 数据备份

所有数据在 `./data/` 目录下：

```bash
# 备份
tar -czf backup-$(date +%Y%m%d).tar.gz data/

# 恢复
tar -xzf backup-20260421.tar.gz
```

### 数据目录说明

```
data/
├── wechat.db          # SQLite 数据库（文章、分类、账号等）
├── kv/                # KV 存储（登录凭证、Cookie）
│   └── cookie/        # 微信登录 Cookie
├── excel/             # 上传的 Excel 文件
└── logs/              # 请求日志（调试模式时）
```

---

## 八、常见问题

### Q: 构建失败 `better-sqlite3` 编译错误

确保 Docker 使用了正确的构建阶段，Dockerfile 已包含 `python3 make g++`。如果拉取代码后重新构建：

```bash
docker compose build --no-cache
```

### Q: 页面能打开但微信扫码登录失败

1. 确认域名已正确解析到服务器
2. 确认 Nginx 反向代理配置正确
3. 检查 Cookie 写入：浏览器 F12 → Application → Cookies，确认域名匹配

### Q: 端口被占用

修改 `.env` 中的 `PORT` 为其他端口，然后重启：

```bash
docker compose down
docker compose up -d
```

### Q: 数据库迁移

应用启动时会自动检测并执行数据库迁移（`server/database/index.ts`），无需手动操作。

### Q: 容器内时区不正确

在 `docker-compose.yml` 的 `environment` 中添加：

```yaml
- TZ=Asia/Shanghai
```

并在 `volumes` 中添加：

```yaml
- /etc/localtime:/etc/localtime:ro
```
